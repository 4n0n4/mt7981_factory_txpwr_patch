```
root@OpenWrt:/tmp# ./mt7981_factory_txpwr_patch.sh
Usage:

    Show zones (2.4/5/6 GHz):
    ./mt7981_factory_txpwr_patch.sh [-f factory.bin] [-L en|ru]

    Patch zones:
    ./mt7981_factory_txpwr_patch.sh -p <preset> [-f factory.bin] [-b band] [-L en|ru]

    Generate presets from dump:
    ./mt7981_factory_txpwr_patch.sh -g -f factory.bin [-p name] [-b band] [-L en|ru]

Flags:
  -f FILE    - Factory input file (default: auto-dump "Factory" partition
               from /proc/mtd into /tmp/factory_dump.bin)
  -p NAME    - preset name (used as "preset_NAME_band")
  -b BAND    - band: 2g | 5g | 6g | all (default: all)
  -g         - generate presets from file
  -L LANG    - interface language: en | ru (default: en)
  -h         - help

Available presets:
  ax3000t
  rax3000me
  wbr3000uax
  wr3000p
root@OpenWrt:/tmp# ./mt7981_factory_txpwr_patch.sh -L ru
Использование:

    Показать зоны (2.4/5/6 ГГц):
    ./mt7981_factory_txpwr_patch.sh [-f factory.bin] [-L en|ru]

    Патчить зоны:
    ./mt7981_factory_txpwr_patch.sh -p <preset> [-f factory.bin] [-b band] [-L en|ru]

    Генерировать пресеты из дампа:
    ./mt7981_factory_txpwr_patch.sh -g -f factory.bin [-p name] [-b band] [-L en|ru]

Флаги:
  -f FILE    - входной файл Factory (по умолчанию: автопоиск раздела "Factory"
               в /proc/mtd и дамп в /tmp/factory_dump.bin)
  -p NAME    - имя пресета (используется как "preset_NAME_band")
  -b BAND    - диапазон: 2g | 5g | 6g | all (по умолчанию: all)
  -g         - режим генерации пресетов из файла
  -L LANG    - язык интерфейса: en | ru (по умолчанию: en)
  -h         - помощь

Доступные пресеты:
  ax3000t
  rax3000me
  wbr3000uax
  wr3000p
```
